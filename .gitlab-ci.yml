stages:          # List of stages for jobs, and their order of execution
  - build
  - deploy

build-job:       # This job runs in the build stage, which runs first.
  stage: build
  tags:
    - python
  script:
    - echo "Compiling the code..."
    - echo "Compile complete."
  artifacts:
    name: "python.zip"
    paths:
      - ./timebank
      - ./run.py
      - ./requirements.txt
  only:
    - main

deploy-job:
  stage: deploy
  tags:
    - python
  before_script:
  - apk add openssh-client
  - eval $(ssh-agent -s)
  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  script:
    - ssh -o StrictHostKeyChecking=no automation@157.245.27.101 "cd ~; touch test.txt"
    - ssh -o StrictHostKeyChecking=no automation@157.245.27.101 "cd /home/automation; unzip python.zip"